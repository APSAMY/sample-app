apiVersion: apps/v1
kind: Deployment
metadata:
  name: development-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: dev-app
  template:
    metadata:
      labels:
        app: dev-app
    spec:
      containers:
      - name: dev-container
        image: <todo>
        ports:
        - containerPort: 8080
          # Replace <todo> with the container image name
spec:
  containers:
    - name: sample-app
      image: gcr.io/qwiklabs-gcp-04-01fd04a0701f/sample-app:v1.0  # Replace with actual project ID

